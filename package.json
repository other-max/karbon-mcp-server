{
  "name": "karbon-server",
  "version": "0.1.0",
  "description": "MCP server for Karbon API integration",
  "type": "module",
  "main": "build/index.js",
  "scripts": {
    "build": "npx tsc && node -e \"require('fs').chmodSync('build/index.js', '755')\"",
    "dev": "npx tsc --watch",
    "start": "node build/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^0.5.0",
    "axios": "^1.6.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  }
}
